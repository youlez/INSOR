jQuery(document).ready(function ($) {
    $('.proinput form').attr('aria-label', 'Buscador');
    $('.proinput input').attr('aria-label', 'Buscador');
    $('.proinput .orig').attr('placeholder', 'Buscar');
    $('.promagnifier').attr('aria-label', 'Botón buscar');
    $('.left .sr-only').text('Anterior contenido');
    $('.right .sr-only').text('Siguiente contenido');
});